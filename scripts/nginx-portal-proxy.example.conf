# 公网仍显示旧前端时，多半是 Nginx 未把请求交给 Node，或 root 指向了旧目录。
# 推荐：整站反代到本机 Node（与 PM2 中 universal-shop 监听的端口一致，默认 3000）
#
# server {
#   listen 443 ssl;
#   server_name eshop.cider-bar.com;
#   location / {
#     proxy_pass http://127.0.0.1:3000;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#   }
# }
#
# 若必须用静态 root，请保证与部署目录一致，例如：
#   root /home/app/universal-shop-v1.0.0/dist/portal;  # 错误：会丢 /portal 前缀路由
# 门户 base 为 /portal/，通常仍建议反代整站到 Node，由 Express 提供 dist。
